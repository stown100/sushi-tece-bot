# 🍽️ Telegram-бот для заказа еды

Telegram-бот для заказа еды из кафе (суши, роллы, бургеры, напитки) с полным функционалом внутри Telegram.

## 📋 Функционал

- ✅ Выбор категорий товаров (Наборы, Роллы, Бургеры, Напитки)
- ✅ Выбор товаров из категорий
- ✅ Добавление нескольких товаров в корзину
- ✅ Просмотр корзины с итоговой суммой
- ✅ Оформление заказа
- ✅ Запрос контакта пользователя
- ✅ Отправка заказа администратору
- ✅ Управление состояниями через FSM

## 🚀 Установка и запуск

### 1. Клонирование и установка зависимостей

```bash
# Установка зависимостей
pip3 install -r requirements.txt
# или
python3 -m pip install -r requirements.txt
```

**Важно:** Убедитесь, что используете Python 3.8 или выше. Проверьте версию:
```bash
python3 --version
```

### 2. Настройка конфигурации

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Заполните `.env` файл:

```env
BOT_TOKEN=your_bot_token_here
ADMIN_ID=your_admin_id_here
```

**Как получить BOT_TOKEN:**
1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям
4. Скопируйте полученный токен в `.env`

**Как получить ADMIN_ID:**

**Способ 1 (через бота):**
1. Найдите [@userinfobot](https://t.me/userinfobot) в Telegram
2. Отправьте команду `/start`
3. Скопируйте ваш ID в `.env`

**Способ 2 (через вашего бота):**
1. Запустите бота
2. Отправьте команду `/myid` вашему боту
3. Скопируйте ID из ответа в `.env`

### 3. Запуск бота

```bash
python3 main.py
```

**Примечание:** Если команда `python` указывает на Python 2.x, используйте `python3` для запуска.

## 📁 Структура проекта

```
sushi-tece-bot/
├── main.py                 # Точка входа, запуск бота
├── config.py               # Конфигурация (токен, ID администратора, цены)
├── data.py                 # Данные о товарах (категории и товары)
├── states.py               # FSM состояния
├── keyboards.py            # Клавиатуры (InlineKeyboard)
├── handlers/               # Обработчики
│   ├── __init__.py
│   ├── start.py           # Обработчик /start
│   ├── categories.py      # Обработчики категорий и товаров
│   ├── cart.py            # Обработчики корзины
│   └── order.py           # Обработчики заказов
├── services/               # Бизнес-логика
│   ├── __init__.py
│   ├── cart.py            # Сервис корзины
│   └── order.py           # Сервис заказов
├── requirements.txt        # Зависимости
├── .env.example           # Пример конфигурации
└── README.md              # Документация
```

## 🔧 Настройка товаров

Товары настраиваются в файле `data.py`. Каждый товар имеет свое название и индивидуальную цену.

Для добавления новых категорий или товаров:

```python
PRODUCTS = {
    "Новая категория": [
        {"name": "Товар 1", "price": 500},
        {"name": "Товар 2", "price": 600},
        {"name": "Товар 3", "price": 550},
        # ...
    ],
}
```

**Важно:**
- Каждый товар должен быть словарем с ключами `name` и `price`
- Цена указывается в рублях (целое число)
- У разных товаров могут быть одинаковые цены
- В каждой категории должно быть 8 товаров (можно изменить)

## 📱 Использование бота

### Для клиентов:

1. **Запуск**: Отправьте `/start` боту
2. **Выбор категории**: Нажмите на кнопку категории
3. **Выбор товара**: Нажмите на товар для добавления в корзину
4. **Добавление товаров**: Используйте "Добавить ещё" для выбора других товаров
5. **Просмотр корзины**: Нажмите "🛒 Корзина" или "🛒 Заказать"
6. **Оформление заказа**: Нажмите "✅ Заказать" и подтвердите заказ
7. **Отправка контакта**: Отправьте контакт или номер телефона

### Для администратора:

1. **Просмотр всех заказов**: Отправьте команду `/orders`
2. **Просмотр деталей заказа**: Нажмите на заказ в списке
3. **Изменение статуса**: Используйте кнопки для изменения статуса заказа:
   - 🆕 Новый
   - ⏳ В обработку
   - ✅ Завершить
   - ❌ Отменить

## 🛠️ Технологии

- **Python 3.8+**
- **aiogram 3.4.1** - асинхронный фреймворк для Telegram Bot API
- **python-dotenv** - загрузка переменных окружения
- **FSM (Finite State Machine)** - управление состояниями диалога

## 📝 Особенности реализации

- ✅ Все кнопки через InlineKeyboard
- ✅ FSM для управления состояниями
- ✅ Корзина хранится в памяти (можно легко заменить на БД)
- ✅ Структурированный код с разделением на модули
- ✅ Легко расширяемая структура для добавления новых категорий и товаров
- ✅ Обработка ошибок и валидация данных

## 🔄 Расширение функционала

### Добавление новой категории

1. Откройте `data.py`
2. Добавьте категорию в словарь `PRODUCTS`
3. Добавьте цену в `config.py` (если нужно)

### Замена хранения корзины на БД

Замените `services/cart.py` на версию с использованием БД (SQLite, PostgreSQL и т.д.)

## 📄 Лицензия

MIT

## 👤 Автор

Разработано для заказа еды из кафе